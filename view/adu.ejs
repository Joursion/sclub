<% include header.ejs %>

<div class="adu">
    <div class="container">
        <div class="rows">
            <div class="col-md-9 col-ms-9 admin_user">
                <% users.forEach(function (u) { %>

                <button></button>
                <% }); %>
            </div>
        </div>
    </div>
</div>

<% include good.ejs%>